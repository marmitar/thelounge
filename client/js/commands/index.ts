import {input as collapse} from "./collapse.ts";
import {input as expand} from "./expand.ts";
import {input as join} from "./join.ts";
import {input as search} from "./search.ts";

export const commands = {
	collapse: collapse,
	expand: expand,
	join: join,
	search: search,
};
